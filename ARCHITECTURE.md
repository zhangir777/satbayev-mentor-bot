# 🏗 Архитектура проекта «ИИ-наставник первокурсника Satbayev University»

## 1. Общая схема работы

```
Студент (Telegram)
    ↓ вопрос
Telegram Bot API
    ↓
bot.py (Python, async)
    ↓ запрос
RAG Engine (rag_engine.py)
    ├── Эмбеддинг запроса → sentence-transformers (all-MiniLM-L6-v2)
    ├── Поиск в ChromaDB → top-3 релевантных чанка
    └── Формирование контекста
    ↓ контекст + вопрос
Google Gemini API (gemini-2.0-flash)
    ↓ сгенерированный ответ
bot.py → Telegram → Студент
```

## 2. Компоненты системы

### 2.1 Telegram Bot API (bot.py)
**Назначение:** Интерфейс взаимодействия со студентами.

- Используется библиотека `python-telegram-bot` v20+ (асинхронная версия)
- Обрабатывает команды `/start` и `/help`
- Предоставляет клавиатуру с быстрыми кнопками по основным темам
- Показывает индикатор набора текста во время обработки
- Обрабатывает ошибки и отправляет понятные сообщения на русском языке

### 2.2 RAG Engine (rag_engine.py)
**Назначение:** Поиск релевантной информации в базе знаний.

Класс `RAGEngine` реализует полный цикл RAG:
- **Загрузка:** чтение .md файлов → разбиение на чанки → создание эмбеддингов → сохранение в ChromaDB
- **Поиск:** эмбеддинг запроса → векторный поиск → возврат top-k результатов
- **Контекст:** объединение найденных чанков в единый контекст для LLM

### 2.3 ChromaDB (chroma_db/)
**Назначение:** Векторная база данных для хранения и поиска эмбеддингов.

- Постоянное хранилище на диске (PersistentClient)
- Коллекция `satbayev_knowledge` с метаданными об источниках
- Поддержка пакетной загрузки данных
- Автоматическое создание при первом запуске

### 2.4 Sentence Transformers
**Назначение:** Создание векторных представлений (эмбеддингов) текста.

- Модель: `all-MiniLM-L6-v2` — компактная и быстрая
- Размерность вектора: 384
- Поддержка русского языка через мультиязычные представления

### 2.5 Google Gemini API
**Назначение:** Генерация ответов на естественном языке.

- Модель: `gemini-2.0-flash` — быстрая и бесплатная
- Системный промпт задаёт роль ИИ-наставника
- Контекст из RAG обеспечивает точность ответов
- Ограничение ответа до 4000 символов (лимит Telegram)

### 2.6 База знаний (knowledge_base/)
**Назначение:** Структурированные документы с информацией о Satbayev University.

11 markdown-файлов, покрывающих основные темы:
- Общая информация и расположение
- Руководство университета
- Институты и кафедры
- Учебный процесс и кредитная система
- Система оценок и GPA
- Регистрация на дисциплины
- Службы и сервисы
- Общежитие
- Студенческая жизнь
- Контакты
- FAQ

## 3. Как работает RAG (Retrieval-Augmented Generation)

### Этап загрузки (offline)
```
.md файлы → Чтение текста → Разбиение на чанки (500 символов, перекрытие 100)
    → Создание эмбеддингов (sentence-transformers) → Сохранение в ChromaDB
```

**Детали разбиения на чанки:**
- Размер чанка: 500 символов
- Перекрытие: 100 символов (для сохранения контекста на границах)
- Интеллектуальное разбиение: поиск конца абзаца или предложения

### Этап запроса (online)
```
Вопрос студента → Эмбеддинг вопроса → Векторный поиск в ChromaDB
    → Top-3 релевантных чанка → Контекст + вопрос → Gemini API → Ответ
```

**Процесс поиска:**
1. Вопрос студента преобразуется в вектор через ту же модель эмбеддингов
2. ChromaDB находит 3 ближайших чанка по косинусному расстоянию
3. Найденные чанки объединяются в контекст с указанием источников
4. Контекст и вопрос передаются в Gemini API
5. Gemini генерирует ответ строго на основе предоставленного контекста

## 4. Почему RAG, а не просто промпт?

| Критерий | Только промпт | RAG |
|---|---|---|
| Точность ответов | Низкая — модель может «выдумывать» | Высокая — ответы основаны на реальных данных |
| Масштабируемость | Ограничена контекстным окном LLM | Неограниченный объём базы знаний |
| Актуальность | Данные устаревают с моделью | Легко обновить — достаточно изменить .md файл |
| Стоимость | Больше токенов на каждый запрос | Только релевантные чанки в промпте |
| Прозрачность | Непонятно откуда взялся ответ | Указаны источники информации |

## 5. Стек технологий — обоснование выбора

| Технология | Почему выбрана |
|---|---|
| **Python** | Стандарт для ML/AI проектов, богатая экосистема библиотек |
| **python-telegram-bot v20+** | Официальная async-библиотека, стабильная, хорошо документирована |
| **Google Gemini (gemini-2.0-flash)** | Бесплатный API, быстрая модель, хорошее качество на русском языке |
| **ChromaDB** | Простая встраиваемая векторная БД, не требует отдельного сервера |
| **sentence-transformers** | Высококачественные эмбеддинги, модель all-MiniLM-L6-v2 — компактная и быстрая |
| **python-dotenv** | Безопасное хранение API-ключей вне кода |

## 6. Безопасность

- API-ключи хранятся в `.env` файле, который не попадает в git (`.gitignore`)
- Бот не хранит персональные данные студентов
- Системный промпт запрещает генерацию информации, отсутствующей в базе знаний
- Обработка ошибок не раскрывает внутренние детали системы пользователю
